from "std" import *

vcc_a = supply("VCC")
vcc_b = supply("3V3")

gnd_ref = create component:
    pins: 1
    display: create graphic:
        hpin: 1, "power_reference", -100, 0, 100, display_id=false

at vcc_a
wire right 100
branch:
    wire down 100
    to gnd_ref
wire right 200
to vcc_b
